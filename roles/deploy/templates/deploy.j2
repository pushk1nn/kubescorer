---
kind: Namespace
apiVersion: v1
metadata:
  name: blueteam-{{team}}

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{host}}-{{service}}-scorecheck
  namespace: blueteam-{{team}}
  labels:
    app: {{host}}-{{service}}-scorecheck
spec:
  selector:
    matchLabels:
      app: {{host}}-{{service}}-scorecheck
  template:
    metadata:
      labels:  
        app: {{host}}-{{service}}-scorecheck
        deployment: {{host}}-{{service}}-scorecheck
    spec:
      containers:
        - name: {{host}}-{{service}}-scorecheck
          image: 100.65.3.181:30000/{{host}}-{{service}}-scorecheck:{{ team }}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
kind: Service
apiVersion: v1
metadata:
  name: {{host}}-{{service}}-scorecheck
  namespace: blueteam-{{team}}
spec:
  ports:
    - name: 45281-tcp
      protocol: TCP
      port: 45281
      targetPort: 45281
  selector:
    app: {{host}}-{{service}}-scorecheck
  type: LoadBalancer
